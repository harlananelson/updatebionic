r-matrix
r-mass
r-duckdb
r-tidyverse
r-data.table
r-tidymodels
r-targets
r-tarchetypes
r-gtsummary
r-ggpubr
r-pacman
r-pak
r-renv
r-here
r-janitor
# r-visnetwork
r-curl
r-survminer
r-nanoparquet
#r-sparklyr
#r-tidycensus
r-remotes
r-ranger
r-ggally
r-xgboost
#r-reticulate
r-pdp
r-contrast
r-cardx
r-estimability
r-mvtnorm
r-numDeriv
r-emmeans
r-pillar
#r-rhdfs
r-pkgconfig
r-mgcv
r-matrix
r-glmnet
r-data.tree
r-diagrammer
r-randomforestsrc
r-psych

# Geospatial — install from conda-forge so they pull the conda builds of
# udunits2 / gdal / geos / proj. The conda R toolchain does NOT see the
# apt-installed -dev libraries, so CRAN source builds of units/sf fail
# (see logs/update-bionic.log). setup-user.sh installs this file before the
# GitHub usmapdata/usmap install, so once sf is present here that chain works.
#
# IMPORTANT (2026-05-21): r-sf + r-units pull libgdal-core / libxml2 versions
# that conflict with r-base=4.4.0 in the main env-creation solve. We attempt
# them in a SEPARATE solve in setup-user.sh Part 5 (after the main env exists)
# so the conflict only kills geospatial, not the whole r_env. If the separate
# solve also fails, usmap/usmapdata GitHub installs may fail too — but
# everything non-geospatial works. Keep them OUT of the main spec list below.
# r-units
# r-sf